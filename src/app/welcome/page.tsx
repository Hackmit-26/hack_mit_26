import { WelcomeScreen } from "@/components/welcome/WelcomeScreen";

/** Same screen as `/`, kept so the join flow's "done" step has a target. */
export default function WelcomePage() {
  return <WelcomeScreen />;
}
