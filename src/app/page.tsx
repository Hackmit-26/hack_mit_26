import { WelcomeScreen } from "@/components/welcome/WelcomeScreen";

/** The front door: welcome, the group, the invite code, the four chapters. */
export default function HomePage() {
  return <WelcomeScreen />;
}
